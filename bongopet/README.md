# BongoPet updates

This directory hosts the signed BongoPet update manifest and installers.
Do not replace `latest.json` until every referenced installer and `.sig` file
has been uploaded successfully.
